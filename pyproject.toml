[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "bluos-controller"
version = "1.4.1"
description = "A command-line controller for BluOS devices on macOS"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [{name = "tbaur"}]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Multimedia :: Sound/Audio",
]

[project.scripts]
bluos-controller = "main:main"

[tool.setuptools]
py-modules = [
    "cli",
    "config",
    "constants",
    "controller",
    "keychain",
    "lsdp",
    "main",
    "models",
    "network",
    "utils",
    "validators",
]

[project.urls]
Homepage = "https://github.com/tbaur/bluos-controller"
Repository = "https://github.com/tbaur/bluos-controller"
Issues = "https://github.com/tbaur/bluos-controller/issues"

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
